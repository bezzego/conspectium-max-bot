# 🐳 Docker Setup для Conspectium

Проект полностью обернут в Docker и состоит из трех контейнеров:
- **db** - PostgreSQL база данных
- **web** - FastAPI веб-приложение (Python)
- **bot** - MAX бот (Node.js)

## 🚀 Быстрый старт

### 1. Подготовка

Убедитесь, что у вас установлены:
- Docker
- Docker Compose

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (или скопируйте из `.env.example`):

```env
# База данных (будет использоваться из docker-compose, но можно переопределить)
DATABASE_URL=postgresql://postgres:postgres@db:5432/conspectium

# Google Gemini API
GOOGLE_API_KEY=your_audio_conspect_api_key
GOOGLE_API_KEY_TEXT=your_text_conspect_api_key

# JWT и безопасность
JWT_SECRET_KEY=your_jwt_secret_key_min_32_characters
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=168

# MAX Bot (Node.js)
MAX_BOT_TOKEN=your_max_bot_token_here
MAX_WELCOME_MESSAGE=Привет! Я бот Макса. Чем могу помочь?

# Окружение
ENVIRONMENT=production

# Хранилище файлов
STORAGE_PATH=./var/audio
AUDIO_STORAGE_DIR=/app/var/audio
```

### 3. Запуск всех сервисов

```bash
docker-compose up -d
```

Это запустит:
- Базу данных PostgreSQL на порту 5432 (внутренний)
- Веб-приложение на порту 8000
- MAX бота (Node.js)

### 4. Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только веб-приложение
docker-compose logs -f web

# Только бот
docker-compose logs -f bot

# Только база данных
docker-compose logs -f db
```

### 5. Остановка

```bash
docker-compose down
```

Для удаления всех данных (включая базу данных):

```bash
docker-compose down -v
```

## 🔧 Управление сервисами

### Запуск только веб-приложения

```bash
docker-compose up -d db web
```

### Запуск только бота

```bash
docker-compose up -d db web bot
```

### Пересборка образов

```bash
# Пересобрать все
docker-compose build

# Пересобрать только веб-приложение
docker-compose build web

# Пересобрать только бота
docker-compose build bot

# Пересобрать и запустить
docker-compose up -d --build
```

### Применение миграций базы данных

Миграции применяются автоматически при запуске веб-приложения через `docker-entrypoint.sh`.

Для ручного применения:

```bash
docker-compose exec web python -m alembic upgrade head
```

## 📁 Структура Docker

```
conspectium-max-bot/
├── docker-compose.yml      # Конфигурация всех сервисов
├── Dockerfile              # Образ для веб-приложения (Python)
├── Dockerfile.bot          # Образ для MAX бота (Node.js)
├── docker-entrypoint.sh    # Скрипт запуска веб-приложения
└── bot/                    # Код MAX бота (Node.js)
    ├── max.js
    ├── package.json
    └── README.md
```

## 🌐 Доступ к сервисам

- **Веб-приложение**: http://localhost:8000
- **API документация**: http://localhost:8000/api/docs
- **База данных**: localhost:5432 (только из контейнеров)

## 🔍 Отладка

### Вход в контейнер

```bash
# Веб-приложение
docker-compose exec web bash

# Бот
docker-compose exec bot bash

# База данных
docker-compose exec db psql -U postgres -d conspectium
```

### Проверка статуса

```bash
docker-compose ps
```

### Просмотр использования ресурсов

```bash
docker stats
```

## 🛠️ Разработка

### Локальная разработка с hot-reload

Для веб-приложения можно использовать volume для автоматической перезагрузки:

```yaml
# В docker-compose.yml уже настроены volumes для var/audio и var/avatars
```

Для разработки бота код монтируется как volume, поэтому изменения применяются сразу.

### Тестирование

```bash
# Запустить тесты в контейнере веб-приложения
docker-compose exec web poetry run pytest
```

## 📝 Примечания

1. **База данных**: Данные сохраняются в Docker volume `pgdata`. При удалении volume все данные будут потеряны.

2. **Файлы**: Аудио файлы и аватары сохраняются в `./var/audio` и `./var/avatars` на хосте.

3. **Переменные окружения**: Все переменные из `.env` автоматически загружаются в контейнеры через `env_file`.

4. **Health checks**: Все сервисы имеют health checks для правильного порядка запуска.

5. **Сеть**: Все контейнеры находятся в одной Docker сети и могут обращаться друг к другу по именам сервисов (db, web, bot).

