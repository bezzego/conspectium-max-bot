# 🎓 Конспектиум — AI-платформа для создания конспектов и тестов

<div align="center">

**Умная платформа для автоматического создания конспектов из аудио и текста с помощью Google Gemini AI**

[![FastAPI](https://img.shields.io/badge/FastAPI-005571?style=for-the-badge&logo=fastapi)](https://fastapi.tiangolo.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)](https://www.postgresql.org/)
[![Python](https://img.shields.io/badge/Python-3776AB?style=for-the-badge&logo=python&logoColor=white)](https://www.python.org/)

</div>

---

## 📋 Содержание

- [🚀 Быстрый старт](#-быстрый-старт)
  - [🐳 Запуск через Docker (Рекомендуется)](#-запуск-через-docker-рекомендуется)
  - [💻 Локальная разработка (без Docker)](#-локальная-разработка-без-docker)
- [⚙️ Установка и настройка](#️-установка-и-настройка)
- [🌐 Работа с веб-приложением](#-работа-с-веб-приложением)
- [📱 Основные возможности](#-основные-возможности)
- [📦 Используемые библиотеки и технологии](#-используемые-библиотеки-и-технологии)
  - [🐍 Python библиотеки (Backend)](#-python-библиотеки-backend)
  - [📦 Node.js библиотеки (MAX Bot)](#-nodejs-библиотеки-max-bot)
- [🗂️ Структура проекта](#️-структура-проекта)
- [🛠️ Разработка](#️-разработка)
- [📚 API документация](#-api-документация)
- [🔒 Безопасность](#-безопасность)
- [🐛 Решение проблем](#-решение-проблем)
- [📞 Поддержка](#-поддержка)
- [📄 Лицензия](#-лицензия)

---

## 🚀 Быстрый старт

### 🐳 Запуск через Docker (Рекомендуется)

**Самый простой способ запустить весь проект (сайт + бот):**

#### Шаг 1: Подготовка окружения

**Требования:**
- Docker версии 20.10+
- Docker Compose версии 2.0+
- Минимум 2GB свободной RAM
- Минимум 5GB свободного места на диске

**Проверка установки:**
```bash
docker --version
docker-compose --version
```

#### Шаг 2: Клонирование репозитория

```bash
git clone <repository-url>
cd conspectium-max-bot
```

#### Шаг 3: Создание файла конфигурации `.env`

Создайте файл `.env` в корне проекта:

```bash
# Если есть .env.example, скопируйте его
cp .env.example .env

# Или создайте новый файл
touch .env
```

#### Шаг 4: Настройка переменных окружения

Откройте файл `.env` и заполните следующие переменные:

```env
# ============================================
# БАЗА ДАННЫХ (для Docker используется автоматически)
# ============================================
# В Docker эти настройки переопределяются автоматически
# DATABASE_URL будет: postgresql://postgres:postgres@db:5432/conspectium
DATABASE_URL=postgresql://postgres:postgres@db:5432/conspectium

# ============================================
# GOOGLE GEMINI API (ОБЯЗАТЕЛЬНО)
# ============================================
# Получите ключи на https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_for_audio
GOOGLE_API_KEY_TEXT=your_gemini_api_key_for_text

# ============================================
# JWT И БЕЗОПАСНОСТЬ (ОБЯЗАТЕЛЬНО)
# ============================================
# Сгенерируйте секретный ключ:
# openssl rand -hex 32
JWT_SECRET_KEY=your_super_secret_jwt_key_min_32_characters_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=168

# ============================================
# MAX БОТ (ОБЯЗАТЕЛЬНО для работы бота)
# ============================================
# Получите токен бота в панели управления MAX
MAX_BOT_TOKEN=your_max_bot_token_here
MAX_WELCOME_MESSAGE=Привет! Я бот Conspectium. Чем могу помочь?

# ============================================
# НАСТРОЙКИ ПРИЛОЖЕНИЯ
# ============================================
ENVIRONMENT=production
# или development для разработки

# Хранилище файлов (опционально, есть значения по умолчанию)
STORAGE_PATH=./var/audio
AUDIO_STORAGE_DIR=/app/var/audio

# ============================================
# API URL для бота (опционально)
# ============================================
# Бот будет использовать этот URL для обращения к API
# По умолчанию: http://web:8000 (внутренний адрес в Docker)
API_BASE_URL=http://web:8000/api
WEB_API_URL=http://web:8000
```

**Важно:**
- `GOOGLE_API_KEY` и `GOOGLE_API_KEY_TEXT` — обязательны для генерации конспектов
- `JWT_SECRET_KEY` — обязателен для безопасности (минимум 32 символа)
- `MAX_BOT_TOKEN` — обязателен для работы MAX бота

**Генерация секретных ключей:**
```bash
# Генерация JWT_SECRET_KEY
openssl rand -hex 32

# Или через Python
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

#### Шаг 5: Запуск всех сервисов

**Запуск в фоновом режиме:**
```bash
docker-compose up -d
```

**Что происходит при запуске:**

1. **База данных PostgreSQL** (`db`):
   - Создается контейнер с PostgreSQL 15
   - Автоматически создается база данных `conspectium`
   - Данные сохраняются в Docker volume `pgdata`
   - Health check проверяет готовность БД

2. **Веб-приложение** (`web`):
   - Собирается Docker образ с Python 3.11
   - Устанавливаются все зависимости через Poetry
   - Ожидается готовность базы данных
   - **Автоматически применяются миграции Alembic**
   - Запускается Gunicorn с 4 воркерами Uvicorn
   - Приложение доступно на порту 8000

3. **MAX бот** (`bot`):
   - Собирается Docker образ с Node.js 18
   - Устанавливаются зависимости через npm
   - Ожидается готовность веб-приложения
   - Запускается бот из файла `bot/max.js`
   - Код бота монтируется как volume для hot-reload

**Время первого запуска:** 3-5 минут (зависит от скорости интернета для загрузки образов)

#### Шаг 6: Проверка статуса

**Проверка работы всех сервисов:**
```bash
docker-compose ps
```

Ожидаемый вывод:
```
NAME                    COMMAND                  SERVICE   STATUS         PORTS
conspectium-max-bot-db-1   "docker-entrypoint.s…"   db        Up (healthy)   5432/tcp
conspectium-max-bot-web-1  "/app/docker-entrypoi…"  web       Up (healthy)   0.0.0.0:8000->8000/tcp
conspectium-max-bot-bot-1  "npm start"              bot       Up              
```

**Проверка логов:**
```bash
# Все сервисы
docker-compose logs -f

# Только веб-приложение
docker-compose logs -f web

# Только бот
docker-compose logs -f bot

# Только база данных
docker-compose logs -f db

# Последние 100 строк
docker-compose logs --tail=100
```

**Проверка здоровья сервисов:**
```bash
# Проверка веб-приложения
curl http://localhost:8000/api/health

# Должен вернуть: {"status":"ok"}
```

#### Шаг 7: Доступ к приложению

**Веб-интерфейс:**
- Главная страница: http://localhost:8000/front/html/main.html
- API документация (Swagger): http://localhost:8000/docs
- API документация (ReDoc): http://localhost:8000/redoc

**MAX бот:**
- Бот автоматически запускается и подключается к MAX API
- Проверьте логи бота: `docker-compose logs -f bot`
- Должно быть сообщение: `✅ Бот успешно запущен и готов к работе!`

#### Шаг 8: Первое использование

1. **Откройте сайт:** http://localhost:8000/front/html/main.html
2. **Зарегистрируйтесь** через форму регистрации
3. **Создайте конспект** из текста или аудио
4. **Протестируйте бота** в MAX мессенджере

---

### 🔧 Управление Docker контейнерами

#### Остановка сервисов

```bash
# Остановка всех сервисов (данные сохраняются)
docker-compose stop

# Остановка и удаление контейнеров (данные сохраняются)
docker-compose down

# Остановка и удаление контейнеров + volumes (ВНИМАНИЕ: удалит все данные БД!)
docker-compose down -v
```

#### Перезапуск сервисов

```bash
# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart web
docker-compose restart bot
docker-compose restart db
```

#### Обновление кода и миграций

**После изменений в коде (включая новые миграции):**

```bash
# 1. Остановить все сервисы (данные БД сохраняются)
docker-compose down

# 2. Пересобрать образы с новым кодом
docker-compose build

# 3. Запустить сервисы заново
# Миграции применятся автоматически при запуске web-контейнера
docker-compose up -d

# 4. Проверить логи, что миграции применились успешно
docker-compose logs web | grep -i migration
```

**Быстрое обновление (без остановки):**

Если изменился только код (без миграций), можно пересобрать и перезапустить только нужный сервис:

```bash
# Пересобрать и перезапустить только веб-приложение
docker-compose up -d --build web

# Пересобрать и перезапустить только бота
docker-compose up -d --build bot

# Пересобрать и перезапустить все сервисы
docker-compose up -d --build
```

**Обновление с применением миграций вручную:**

Если нужно применить миграции вручную (например, для проверки):

```bash
# Применить миграции в запущенном контейнере
docker-compose exec web python -m alembic upgrade head

# Проверить текущую версию миграций
docker-compose exec web python -m alembic current

# Просмотреть историю миграций
docker-compose exec web python -m alembic history
```

**Полная пересборка (если что-то пошло не так):**

```bash
# Остановить и удалить контейнеры
docker-compose down

# Пересобрать образы без использования кеша
docker-compose build --no-cache

# Запустить заново
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

**Важно:**
- Миграции применяются **автоматически** при запуске `web` контейнера через `docker-entrypoint.sh`
- Данные базы данных сохраняются в Docker volume `pgdata` и не удаляются при `docker-compose down`
- Для удаления всех данных используйте `docker-compose down -v` (⚠️ **осторожно!**)

#### Просмотр использования ресурсов

```bash
# Использование ресурсов всеми контейнерами
docker stats

# Или для конкретных контейнеров
docker stats conspectium-max-bot-web-1 conspectium-max-bot-bot-1
```

---

### 🛠️ Отладка и решение проблем

#### Вход в контейнеры

```bash
# Войти в контейнер веб-приложения
docker-compose exec web bash

# Войти в контейнер бота
docker-compose exec bot sh

# Подключиться к базе данных
docker-compose exec db psql -U postgres -d conspectium
```

#### Применение миграций вручную

Миграции применяются автоматически, но можно применить вручную:

```bash
docker-compose exec web python -m alembic upgrade head
```

#### Просмотр логов ошибок

```bash
# Логи с временными метками
docker-compose logs -f --timestamps

# Только ошибки веб-приложения
docker-compose logs web 2>&1 | grep -i error

# Только ошибки бота
docker-compose logs bot 2>&1 | grep -i error
```

#### Очистка и пересборка

```bash
# Удалить все контейнеры, сети и volumes
docker-compose down -v

# Удалить неиспользуемые образы
docker image prune -a

# Пересобрать образы с нуля (без кеша)
docker-compose build --no-cache

# Запустить заново
docker-compose up -d
```

#### Проверка подключения между сервисами

```bash
# Проверить, что веб-приложение видит базу данных
docker-compose exec web ping -c 3 db

# Проверить, что бот видит веб-приложение
docker-compose exec bot wget -O- http://web:8000/api/health
```

---

### 📝 Важные замечания

1. **База данных:**
   - Данные сохраняются в Docker volume `pgdata`
   - При `docker-compose down -v` все данные будут удалены
   - Для продакшена рекомендуется использовать внешнюю БД

2. **Файлы:**
   - Аудио файлы сохраняются в `./var/audio` на хосте
   - Аватары сохраняются в `./var/avatars` на хосте
   - Эти папки создаются автоматически

3. **Переменные окружения:**
   - Все переменные из `.env` автоматически загружаются
   - В Docker `DATABASE_URL` переопределяется автоматически
   - Бот использует `http://web:8000` для обращения к API

4. **Hot-reload:**
   - Код бота монтируется как volume, изменения применяются сразу
   - Для веб-приложения нужна пересборка образа

5. **Порты:**
   - Веб-приложение: `8000` (можно изменить в `docker-compose.yml`)
   - База данных: `5432` (только внутри Docker сети)

---

**Подробная документация по Docker:** см. [DOCKER.md](DOCKER.md)

---

### 💻 Локальная разработка (без Docker)

```bash
# 1. Клонируйте репозиторий (если еще не клонирован)
git clone <repository-url>
cd conspectium-max-bot

# 2. Установите зависимости через Poetry
poetry install

# 3. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env файл (см. раздел "Настройка окружения")

# 4. Примените миграции базы данных
poetry run alembic upgrade head

# 5. Запустите сервер
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

После запуска откройте в браузере: **http://127.0.0.1:8000/front/html/main.html**

---

## ⚙️ Установка и настройка

### Требования

- **Python** 3.10+
- **PostgreSQL** 12+
- **Poetry** (менеджер зависимостей)
- **Google Gemini API ключи** (для генерации конспектов)

### Пошаговая установка

#### 1. Установка Poetry (если еще не установлен)

```bash
curl -sSL https://install.python-poetry.org | python3 -
```

Или через pip:
```bash
pip install poetry
```

#### 2. Установка зависимостей проекта

```bash
poetry install
```

Это установит все необходимые пакеты, включая:
- FastAPI и Uvicorn
- SQLAlchemy и Alembic
- Pydantic
- Google Generative AI SDK
- И другие зависимости

#### 3. Настройка базы данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE conspectium;
CREATE USER conspectium_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE conspectium TO conspectium_user;
```

#### 4. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

**Минимальный набор переменных для локальной разработки:**

```env
# Core settings
PROJECT_NAME=Conspectium API
API_V1_PREFIX=/api
SECRET_KEY=2f5ca1e52ff42439c022d00ec83dd534b1500bd0dbf6f428cda19fcc085c0126
ACCESS_TOKEN_EXPIRE_MINUTES=4320
# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/conspectium
DB_HOST=localhost
DB_PORT=5432
DB_NAME=conspectium
DB_USER=postgres
DB_PASSWORD=postgres

# Google Gemini
GOOGLE_API_KEY=AIzaSyD7XPydid3hIVQeg13DHtvLGhv-fqFB1_c
GOOGLE_MODEL=gemini-2.5-flash
GOOGLE_API_KEY_TEXT=AIzaSyAI0V9pknSPb_C86eZs7PjSGB1l00rCzvs

# Storage
AUDIO_STORAGE_DIR=var/audio

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000","https://t.me"]

# Telegram
# MAX Bot
MAX_API_BASE_URL=https://platform-api.max.ru
MAX_BOT_TOKEN=f9LHodD0cOKVvL6Os8H-RlGnfxOkJIqBaRi2lAeLjJO2nDiIg97mfJgbjAd8azQrbz2N-KcbwMk6BebSF7pf
MAX_MINI_APP_URL=https://conspectium-hackflow.ru/


```

#### 5. Применение миграций базы данных

```bash
# Применить все миграции
poetry run alembic upgrade head

# Просмотреть текущую версию
poetry run alembic current

# Создать новую миграцию (после изменения моделей)
poetry run alembic revision --autogenerate -m "описание изменений"
```

#### 6. Запуск сервера

**Для разработки (с автоперезагрузкой):**
```bash
poetry run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Для продакшена:**
```bash
poetry run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

#### 7. Настройка Nginx (для продакшена)

Если вы используете Nginx как обратный прокси, необходимо настроить его для поддержки загрузки файлов большого размера.

**1. Скопируйте конфигурацию Nginx:**
```bash
sudo cp nginx.conf /etc/nginx/sites-available/conspectium
```

**2. Отредактируйте конфигурацию:**
```bash
sudo nano /etc/nginx/sites-available/conspectium
```

Убедитесь, что указаны правильные пути:
- `server_name` - ваш домен
- Пути к статическим файлам (`/front/`, `/img/`)
- Порт FastAPI (обычно `8000`)

**3. Создайте симлинк:**
```bash
sudo ln -s /etc/nginx/sites-available/conspectium /etc/nginx/sites-enabled/
```

**4. Проверьте конфигурацию:**
```bash
sudo nginx -t
```

**5. Перезагрузите Nginx:**
```bash
sudo systemctl reload nginx
```

**Важно:** Убедитесь, что в конфигурации установлено:
- `client_max_body_size 10M;` - для загрузки аватаров (до 5 МБ)
- Увеличенные таймауты для больших загрузок
- Правильные настройки проксирования

---

## 🌐 Работа с веб-приложением

### 🎯 Главная страница (`main.html`)

**URL:** `http://127.0.0.1:8000/front/html/main.html`

**Основные функции:**
- 📊 Просмотр последних созданных конспектов
- 🎤 Быстрая загрузка аудиофайла для создания конспекта
- 🏆 Отображение статистики медалей (золото, серебро, бронза)
- 🔗 Быстрый доступ ко всем разделам приложения

**Как использовать:**
1. При первом посещении появится модальное окно регистрации
2. Заполните форму: Email, Nickname, Password
3. После регистрации вы автоматически войдете в систему
4. На главной странице увидите свои последние конспекты
5. Для создания нового конспекта нажмите "Создай конспект" или загрузите аудио

### 📝 Создание конспекта (`conspect_create.html`)

**URL:** `http://127.0.0.1:8000/front/html/conspect_create.html`

**Способы создания конспекта:**

#### 1. Из аудиофайла 🎵

**Поддерживаемые форматы:**
- MP3, M4A, WAV, OGG, WebM, AAC

**Процесс:**
1. Нажмите кнопку "Загрузить аудио" или перетащите файл
2. Выберите вариант конспекта:
   - 📄 **Полный** — детальный конспект со всеми подробностями
   - 📋 **Краткий** — сжатая версия с основными тезисами
   - 🎯 **Профиль** — специализированный формат
3. Дождитесь обработки (может занять несколько минут для длинных аудио)
4. Конспект автоматически откроется после создания

#### 2. Запись с микрофона 🎤

1. Нажмите кнопку "Записать аудио прямо на сайте"
2. Разрешите доступ к микрофону
3. Начните запись
4. Остановите запись, когда закончите
5. Выберите вариант конспекта
6. Дождитесь обработки

#### 3. Из текста ✍️

1. Вставьте или введите текст в текстовое поле
2. Выберите вариант конспекта
3. Нажмите "Создать конспект"
4. Конспект будет создан мгновенно

**Важно:** При создании конспекта из аудио используется `GOOGLE_API_KEY`, а из текста — `GOOGLE_API_KEY_TEXT`.

### 📚 Список конспектов (`conspect_list.html`)

**URL:** `http://127.0.0.1:8000/front/html/conspect_list.html`

**Функции:**
- 📋 Просмотр всех ваших конспектов
- 🔍 Поиск по названию
- 👁️ Просмотр конспекта в модальном окне
- 🔗 Кнопка "Поделиться" для создания публичной ссылки
- 📱 Сохранение чужих конспектов через публичную ссылку

**Как поделиться конспектом:**
1. Откройте конспект из списка
2. Нажмите кнопку "Поделиться"
3. Скопируйте ссылку
4. Отправьте ссылку другому пользователю

**Как открыть чужой конспект:**
1. На главной странице нажмите "Скинули ссылку?"
2. Вставьте полученную ссылку
3. Нажмите "Открыть"
4. Конспект откроется в режиме просмотра
5. При необходимости нажмите "Сохранить" для добавления в свой список

### ✅ Создание теста (`test_create.html` / `choose_test.html`)

**URL:** `http://127.0.0.1:8000/front/html/choose_test.html`

**Процесс создания теста:**

1. Выберите конспект, на основе которого хотите создать тест
2. Нажмите "Настроить тест"
3. Выберите количество вопросов (от 1 до 20) с помощью ползунка
4. Нажмите "Создать тест"
5. Дождитесь генерации (обычно 1-2 минуты)
6. Тест автоматически откроется для прохождения

**Важно:** Тест создается на основе выбранного конспекта. Чем больше вопросов, тем дольше генерация.

### 📊 Список тестов (`test_list.html`)

**URL:** `http://127.0.0.1:8000/front/html/test_list.html`

**Функции:**
- 📋 Просмотр всех сохраненных тестов
- ▶️ Быстрый запуск теста без перезагрузки страницы
- ✏️ Переименование теста
- 🔗 Поделиться тестом (создание публичной ссылки)

### 🎮 Прохождение теста (`test.html`)

**URL:** `http://127.0.0.1:8000/front/html/test.html?quizId=<id>`

**Процесс:**
1. Выберите тест из списка или откройте по ссылке
2. Читайте вопросы и выбирайте правильные ответы
3. После каждого вопроса нажмите "Далее"
4. В конце теста увидите результаты:
   - Количество правильных ответов
   - Процент выполнения
   - Медаль (если набрали 100%)
5. При необходимости пройдите тест повторно

### 🏆 Турниры (`tournament.html`)

**URL:** `http://127.0.0.1:8000/front/html/tournament.html`

**Функции:**
- 🎯 Участие в турнирах по тестам
- 🥇 Получение медалей за достижения
- 📊 Просмотр статистики участия

### ⚙️ Настройки (`settings.html`)

**URL:** `http://127.0.0.1:8000/front/html/settings.html`

**Доступные настройки:**
- 👤 Редактирование профиля (имя, пол, дата рождения)
- 🔐 Изменение пароля
- 🚪 Выход из аккаунта

**Как изменить пароль:**
1. Введите текущий пароль
2. Введите новый пароль (минимум 8 символов)
3. Подтвердите новый пароль
4. Нажмите "Изменить пароль"

---

## 📱 Основные возможности

### ✨ Ключевые функции

| Функция | Описание |
|---------|----------|
| 🎤 **Загрузка аудио** | Поддержка множества форматов (MP3, M4A, WAV, OGG и др.) |
| 🎙️ **Запись с микрофона** | Встроенная запись аудио прямо в браузере |
| 📝 **Создание из текста** | Быстрое создание конспекта из готового текста |
| 🤖 **AI-генерация** | Использование Google Gemini для создания конспектов |
| 📋 **Варианты конспектов** | Полный, краткий и профильный варианты |
| ✅ **Генерация тестов** | Автоматическое создание тестов из конспектов |
| 🎯 **Настройка тестов** | Выбор количества вопросов (1-20) |
| 🔗 **Публичные ссылки** | Поделиться конспектами и тестами |
| 👥 **Мультипользователь** | Изоляция данных между пользователями |
| 🔐 **Безопасность** | JWT-авторизация, хеширование паролей |
| 🏆 **Медали и достижения** | Система наград за прохождение тестов |

### 🎨 Особенности интерфейса

- **Современный дизайн** с эффектом "liquid glass"
- **Адаптивная верстка** для мобильных устройств
- **Плавные анимации** и переходы
- **Темная тема** по умолчанию
- **Интуитивная навигация**

---

## 📦 Используемые библиотеки и технологии

### 🐍 Python библиотеки (Backend)

Проект использует современный стек Python для создания высокопроизводительного API:

#### **Веб-фреймворк и сервер**
- **FastAPI** (`^0.111.0`) — современный веб-фреймворк для создания API с автоматической документацией, валидацией данных и поддержкой async/await
- **Uvicorn** (`^0.30.0`) — ASGI сервер для запуска FastAPI приложений с поддержкой WebSockets и высокой производительностью
- **Gunicorn** — WSGI HTTP сервер для продакшена, используется в Docker контейнере

#### **База данных и ORM**
- **SQLAlchemy** (`^2.0.30`) — мощный ORM для работы с базами данных, поддерживает асинхронные запросы и сложные связи
- **Alembic** (`^1.13.1`) — система миграций базы данных для SQLAlchemy, позволяет версионировать схему БД
- **psycopg2-binary** (`^2.9.9`) — адаптер PostgreSQL для Python, обеспечивает высокую производительность работы с БД

#### **Валидация и настройки**
- **Pydantic** (через FastAPI) — валидация данных с использованием типов Python, автоматическая генерация схем
- **pydantic-settings** (`^2.3.0`) — управление настройками приложения из переменных окружения и файлов конфигурации

#### **AI и машинное обучение**
- **google-generativeai** (`^0.5.4`) — официальный SDK для работы с Google Gemini AI, используется для генерации конспектов из аудио и текста

#### **Аутентификация и безопасность**
- **PyJWT** (`^2.8.0`) — создание и проверка JWT токенов для аутентификации пользователей
- **passlib** (`^1.7.4`) — библиотека для хеширования паролей с поддержкой различных алгоритмов
- **bcrypt** (`^5.0.0`) — криптографический алгоритм для безопасного хранения паролей

#### **HTTP клиенты**
- **httpx** (`^0.27.0`) — современный асинхронный HTTP клиент для Python, используется для внешних API запросов

#### **Работа с файлами**
- **python-multipart** (`^0.0.9`) — обработка multipart/form-data для загрузки файлов (аудио, изображения)

#### **Инструменты разработки** (dev dependencies)
- **pytest** (`^8.2.0`) — фреймворк для написания и запуска тестов
- **pytest-asyncio** (`^0.23.6`) — поддержка асинхронных тестов в pytest
- **faker** (`^25.0.0`) — генерация тестовых данных
- **ruff** (`^0.4.0`) — быстрый линтер и форматтер для Python
- **black** (`^24.4.2`) — форматтер кода Python
- **mypy** (`^1.9.0`) — статический анализатор типов для Python

### 📦 Node.js библиотеки (MAX Bot)

Бот для мессенджера MAX использует следующие библиотеки:

#### **Основные библиотеки**
- **@maxhub/max-bot-api** (`^0.2.1`) — официальная библиотека для работы с MAX Bot API, обеспечивает взаимодействие с мессенджером MAX
- **dotenv** (`^16.3.1`) — загрузка переменных окружения из `.env` файла для конфигурации бота
- **tamtam** (`^1.0.10`) — дополнительная библиотека для работы с мессенджерами (deprecated, но используется для совместимости)

#### **Встроенные возможности Node.js**
- **fetch API** — встроенный HTTP клиент (Node.js 18+), используется для запросов к веб-API
- **EventEmitter** — для обработки событий бота
- **fs/promises** — асинхронная работа с файловой системой

### 🗄️ База данных

- **PostgreSQL 15** — реляционная база данных для хранения всех данных приложения (конспекты, тесты, пользователи, результаты)

### 🐳 Инфраструктура

- **Docker** — контейнеризация приложения
- **Docker Compose** — оркестрация многоконтейнерного приложения
- **Poetry** — управление зависимостями Python проекта

---

## 🗂️ Структура проекта

```
conspectium-max-bot/
├── 📁 app/                          # Основное приложение
│   ├── 📁 api/                      # API endpoints
│   │   ├── api_v1.py               # Роутер API v1
│   │   ├── deps.py                 # Зависимости (auth, DB)
│   │   └── 📁 endpoints/            # Эндпоинты
│   │       ├── auth.py             # Авторизация
│   │       ├── audio.py            # Загрузка аудио
│   │       ├── conspects.py        # Конспекты
│   │       ├── quizzes.py           # Тесты
│   │       ├── jobs.py              # Задачи генерации
│   │       └── health.py            # Health check
│   ├── 📁 core/                     # Конфигурация
│   │   └── config.py               # Настройки приложения
│   ├── 📁 db/                       # База данных
│   │   └── session.py              # SQLAlchemy session
│   ├── 📁 models/                   # SQLAlchemy модели
│   │   ├── user.py                 # Пользователь
│   │   ├── conspect.py             # Конспект
│   │   ├── quiz.py                 # Тест
│   │   ├── audio.py                # Аудио
│   │   ├── generation.py           # Задачи генерации
│   │   └── enums.py                # Перечисления
│   ├── 📁 schemas/                  # Pydantic схемы
│   │   ├── user.py                 # Схемы пользователя
│   │   ├── conspect.py             # Схемы конспекта
│   │   ├── quiz.py                 # Схемы теста
│   │   └── job.py                  # Схемы задач
│   ├── 📁 services/                 # Бизнес-логика
│   │   ├── auth.py                 # Авторизация
│   │   ├── security.py             # Безопасность (JWT, пароли)
│   │   ├── generation.py           # Генерация конспектов/тестов
│   │   ├── storage.py              # Работа с файлами
│   │   ├── telegram.py             # Интеграция с Telegram
│   │   └── 📁 ai/
│   │       └── gemini.py            # Клиент Google Gemini
│   └── main.py                     # Точка входа FastAPI
│
├── 📁 front/                        # Frontend
│   ├── 📁 html/                     # HTML страницы
│   │   ├── main.html               # Главная
│   │   ├── conspect_create.html    # Создание конспекта
│   │   ├── conspect_list.html      # Список конспектов
│   │   ├── test_create.html        # Создание теста
│   │   ├── test_list.html          # Список тестов
│   │   ├── test.html               # Прохождение теста
│   │   ├── settings.html           # Настройки
│   │   └── ...
│   ├── 📁 js/                       # JavaScript
│   │   ├── app.js                  # Основной API клиент
│   │   ├── script.js               # Общая логика
│   │   ├── conspect_create.js      # Создание конспекта
│   │   ├── test.js                 # Прохождение теста
│   │   └── ...
│   └── 📁 css/                      # Стили
│       ├── style.css               # Основные стили
│       ├── main.css                # Главная страница
│       ├── liquid_glass.css        # Эффект glass
│       └── ...
│
├── 📁 alembic/                      # Миграции БД
│   ├── env.py                      # Конфигурация Alembic
│   └── 📁 versions/                 # Файлы миграций
│
├── 📁 tests/                        # Тесты
│   ├── test_generation_service.py
│   ├── test_quiz_models.py
│   └── ...
│
├── 📁 var/                          # Хранилище файлов
│   └── 📁 audio/                    # Загруженные аудио
│
├── 📄 pyproject.toml                # Зависимости Poetry
├── 📄 alembic.ini                   # Конфигурация Alembic
├── 📄 Dockerfile                    # Docker образ
├── 📄 docker-compose.yml             # Docker Compose
└── 📄 README.md                     # Этот файл
```

---

## 🛠️ Разработка

### Полезные команды

```bash
# Запуск тестов
poetry run pytest

# Статический анализ кода
poetry run ruff check .

# Форматирование кода
poetry run black .

# Создание новой миграции
poetry run alembic revision --autogenerate -m "описание"

# Применение миграций
poetry run alembic upgrade head

# Откат миграции
poetry run alembic downgrade -1

# Просмотр текущей версии БД
poetry run alembic current

# Запуск с отладкой
poetry run uvicorn app.main:app --reload --log-level debug
```

### Работа с базой данных

**Подключение к БД через psql:**
```bash
psql -U conspectium_user -d conspectium
```

**Просмотр таблиц:**
```sql
\dt
```

**Просмотр структуры таблицы:**
```sql
\d user
\d conspect
\d quiz
```

### Создание тестового пользователя (для разработки)

```bash
# Через API (требует запущенный сервер)
curl -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "nickname": "testuser",
    "password": "testpassword123",
    "name": "Test User",
    "gender": "male",
    "birth_date": "2000-01-01"
  }'
```

### Отладка

**Включение логирования:**
```python
# В app/core/config.py можно настроить уровень логирования
import logging
logging.basicConfig(level=logging.DEBUG)
```

**Просмотр логов сервера:**
Логи выводятся в консоль при запуске через `uvicorn`.

---

## 📚 API документация

### Автоматическая документация

После запуска сервера доступна интерактивная документация:

- **Swagger UI:** http://127.0.0.1:8000/docs
- **ReDoc:** http://127.0.0.1:8000/redoc

### Основные эндпоинты

#### Авторизация

```
POST   /api/auth/register          # Регистрация
POST   /api/auth/login             # Вход
GET    /api/auth/me                # Текущий пользователь
POST   /api/auth/change-password   # Изменение пароля
```

#### Конспекты

```
GET    /api/conspects              # Список конспектов
POST   /api/conspects              # Создать конспект
GET    /api/conspects/{id}         # Получить конспект
PUT    /api/conspects/{id}         # Обновить конспект
DELETE /api/conspects/{id}         # Удалить конспект
GET    /api/conspects/share/{token} # Публичный конспект
POST   /api/conspects/share/{token}/save # Сохранить публичный конспект
```

#### Тесты

```
GET    /api/quizzes                # Список тестов
POST   /api/quizzes/from-conspect  # Создать тест из конспекта
GET    /api/quizzes/{id}           # Получить тест
GET    /api/quizzes/share/{token}  # Публичный тест
```

#### Аудио

```
POST   /api/audio/upload           # Загрузить аудио
GET    /api/audio/{id}             # Получить информацию об аудио
```

#### Задачи

```
GET    /api/jobs/{id}              # Статус задачи генерации
```

### Примеры использования API

**Регистрация:**
```bash
curl -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "nickname": "username",
    "password": "password123",
    "name": "User Name",
    "gender": "male",
    "birth_date": "2000-01-01"
  }'
```

**Вход:**
```bash
curl -X POST http://127.0.0.1:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "login": "user@example.com",
    "password": "password123"
  }'
```

**Создание конспекта из текста:**
```bash
curl -X POST http://127.0.0.1:8000/api/conspects \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "initial_summary": "Текст для конспекта...",
    "title": "Название конспекта",
    "variants": ["full"]
  }'
```

---

## 🔒 Безопасность

### Реализованные меры

- ✅ **JWT токены** для авторизации
- ✅ **Хеширование паролей** с использованием bcrypt
- ✅ **Валидация данных** через Pydantic
- ✅ **Изоляция данных** между пользователями
- ✅ **HTTPS** для продакшена (требуется настройка)

### Рекомендации для продакшена

1. Используйте сильный `JWT_SECRET_KEY` (минимум 32 символа)
2. Настройте HTTPS с валидным SSL-сертификатом
3. Ограничьте CORS для API
4. Настройте rate limiting
5. Регулярно обновляйте зависимости
6. Используйте переменные окружения для секретов

---

## 🐛 Решение проблем

### Проблема: Не запускается сервер

**Решение:**
- Проверьте, что PostgreSQL запущен
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что порт 8000 свободен

### Проблема: Ошибка миграций

**Решение:**
```bash
# Откатите миграции
poetry run alembic downgrade -1

# Или сбросьте БД (ОСТОРОЖНО: удалит все данные!)
# Затем примените миграции заново
poetry run alembic upgrade head
```

### Проблема: Не работает генерация конспектов

**Решение:**
- Проверьте, что `GOOGLE_API_KEY` установлен
- Убедитесь, что ключ валидный и имеет доступ к Gemini API
- Проверьте логи сервера на наличие ошибок

### Проблема: Бот не отвечает

**Решение:**
- Проверьте, что сервер доступен извне (используйте ngrok для тестирования)

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Изучите логи сервера
3. Проверьте документацию API в `/docs`
4. Создайте issue в репозитории проекта

---

## 📄 Лицензия

См. файл [LICENSE](LICENSE) для подробной информации.

---

<div align="center">

**Сделано с ❤️ для эффективного обучения**

[⬆ Наверх](#-конспектиум--ai-платформа-для-создания-конспектов-и-тестов)

</div>
