name: Telegram Notifications

on:
  push:
  pull_request:
  issues:
    types: [opened, closed, reopened, edited]
  pull_request_review:
    types: [submitted]
  release:
    types: [published, created, edited]
  create:
  delete:
  fork:
  watch:
    types: [started]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Telegram message
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_IDS: ${{ secrets.TG_CHAT_IDS }}
        run: |
          set -e

          EVENT="${{ github.event_name }}"
          ACTOR="${{ github.actor }}"
          REPO="${{ github.repository }}"
          BRANCH="${{ github.ref_name }}"

          # Экранирование для MarkdownV2 (устойчивое и полное)
          escape() {
            printf '%s' "$1" | sed \
              -e 's/\\/\\\\/g' \
              -e 's/_/\\_/g' \
              -e 's/\*/\\*/g' \
              -e 's/\[/\\[/g' \
              -e 's/\]/\\]/g' \
              -e 's/(/\\(/g' \
              -e 's/)/\\)/g' \
              -e 's/~/\\~/g' \
              -e 's/`/\\`/g' \
              -e 's/>/\\>/g' \
              -e 's/#/\\#/g' \
              -e 's/\+/\\+/g' \
              -e 's/-/\\-/g' \
              -e 's/=/\\=/g' \
              -e 's/|/\\|/g' \
              -e 's/{/\\{/g' \
              -e 's/}/\\}/g' \
              -e 's/\./\\./g' \
              -e 's/!/\\!/g'
          }

          SAFE_EVENT="$(escape "$EVENT")"
          SAFE_ACTOR="$(escape "$ACTOR")"
          SAFE_REPO="$(escape "$REPO")"
          SAFE_BRANCH="$(escape "$BRANCH")"

          MESSAGE=""

          case "$EVENT" in
            push)
              COMMIT_MSG="$(echo "${{ github.event.head_commit.message }}" | head -n 1)"
              SAFE_COMMIT="$(escape "$COMMIT_MSG")"
              MESSAGE="🟢 Push в репозиторий:\n${SAFE_REPO}\n\n👤 Автор: ${SAFE_ACTOR}\n🌿 Ветка: ${SAFE_BRANCH}\n💬 Сообщение: ${SAFE_COMMIT}"
              ;;
            pull_request)
              TITLE="${{ github.event.pull_request.title }}"
              ACTION="${{ github.event.action }}"
              SAFE_TITLE="$(escape "$TITLE")"
              SAFE_ACTION="$(escape "$ACTION")"
              MESSAGE="📦 Pull Request: ${SAFE_ACTION}\n${SAFE_TITLE}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            pull_request_review)
              ACTION="${{ github.event.action }}"
              SAFE_ACTION="$(escape "$ACTION")"
              MESSAGE="🗣 Review: ${SAFE_ACTION}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            issues)
              TITLE="${{ github.event.issue.title }}"
              ACTION="${{ github.event.action }}"
              SAFE_TITLE="$(escape "$TITLE")"
              SAFE_ACTION="$(escape "$ACTION")"
              MESSAGE="🗂 Issue: ${SAFE_ACTION}\n${SAFE_TITLE}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            release)
              REL_NAME="${{ github.event.release.name }}"
              SAFE_REL="$(escape "$REL_NAME")"
              MESSAGE="🚀 Новый релиз опубликован:\n${SAFE_REL}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            create)
              REF="${{ github.ref }}"
              SAFE_REF="$(escape "$REF")"
              MESSAGE="✨ Создана новая ветка или тег:\n${SAFE_REF}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            delete)
              REF="${{ github.ref }}"
              SAFE_REF="$(escape "$REF")"
              MESSAGE="🗑 Удалена ветка или тег:\n${SAFE_REF}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            fork)
              MESSAGE="🍴 Новый форк репозитория:\n${SAFE_REPO}\n\n👤 Автор: ${SAFE_ACTOR}"
              ;;
            watch)
              MESSAGE="⭐️ Новая звезда на репозитории:\n${SAFE_REPO}\n\n👤 От: ${SAFE_ACTOR}"
              ;;
            *)
              MESSAGE="📘 Событие: ${SAFE_EVENT}\n\n👤 Автор: ${SAFE_ACTOR}\n📦 Репозиторий: ${SAFE_REPO}"
              ;;
          esac

          # Рассылка в несколько чатов
          if [ -n "$MESSAGE" ]; then
            for CHAT_ID in $TG_CHAT_IDS; do
              echo "Отправляю уведомление в $CHAT_ID"
              curl -s -X POST "https://api.telegram.org/bot$TG_BOT_TOKEN/sendMessage" \
                   -d chat_id="$CHAT_ID" \
                   -d parse_mode="MarkdownV2" \
                   -d text="$MESSAGE" >/dev/null
            done
          fi